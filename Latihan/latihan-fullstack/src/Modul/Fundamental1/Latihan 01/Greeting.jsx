import React from 'react'

const Greeting = (props) => {
    return (
        <div>
            <p>Heloo World, I'm {props.name} yeard Old{props.age}</p>
        </div>
    )
}

export default Greeting
